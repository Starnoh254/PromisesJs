let age = 25
let message = age >= 18 ? "You are old enough to vote " : "You are a"
console.log(message + " " + age)

// Challenge: Use the ternary operator to make this code shorter and more readable
// concise syntax
const isEven = num => num % 2 === 0 ? "Even" : "Odd"

console.log(isEven(239))